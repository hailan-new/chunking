# LLM配置环境变量示例
# 复制此文件为 .env 并填入实际的API密钥

# =============================================================================
# LLM 配置
# =============================================================================

# 通义千问（阿里云百炼）API密钥
DASHSCOPE_API_KEY=sk-your-dashscope-api-key-here

# OpenAI API密钥（如果使用OpenAI）
OPENAI_API_KEY=sk-your-openai-api-key-here

# Claude API密钥（如果使用Claude）
ANTHROPIC_API_KEY=sk-your-anthropic-api-key-here

# LLM功能开关
CHUNKING_LLM_ENABLED=true

# LLM提供商选择 (qwen, openai, claude, custom)
CHUNKING_LLM_PROVIDER=qwen

# LLM模型名称
CHUNKING_LLM_MODEL=qwen-plus

# LLM API基础URL（可选，用于自定义端点）
CHUNKING_LLM_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1

# LLM参数配置
CHUNKING_LLM_TEMPERATURE=0.1
CHUNKING_LLM_MAX_TOKENS=1000
CHUNKING_LLM_TIMEOUT=30
CHUNKING_LLM_RETRY_TIMES=3
CHUNKING_LLM_BATCH_SIZE=20
CHUNKING_LLM_CACHE_ENABLED=true

# =============================================================================
# 文档处理配置
# =============================================================================

# 默认文档处理参数
CHUNKING_MAX_TOKENS=2000
CHUNKING_OVERLAP=200
CHUNKING_STRICT_MAX_TOKENS=false

# =============================================================================
# 使用说明
# =============================================================================

# 1. 将此文件复制为 .env
# 2. 填入实际的API密钥
# 3. 根据需要调整其他配置参数
# 4. 确保 .env 文件不被提交到版本控制系统

# 注意：
# - 环境变量的优先级高于配置文件
# - 布尔值使用 true/false（小写）
# - 数值类型会自动转换
# - 如果不设置某个环境变量，将使用默认配置
